# Module 12 — Strings and formatting

Chay: `dotnet run --project modules/12-strings-and-formatting`

- Common `string` methods
- `StringBuilder` for efficient concatenation
- String interpolation and `string.Format`
