# Module 15 — Delegates, events, lambda

Chay: `dotnet run --project modules/15-delegates-events-lambda`

- `Func<>` and `Action<>`
- Lambda expressions with LINQ-style `Select`
- `event` and multicast invocation
