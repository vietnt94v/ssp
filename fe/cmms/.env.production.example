# Production — set these in Vercel: Project → Settings → Environment Variables
# https://vercel.com/docs/projects/environment-variables

# Must point at the API /api prefix (https://host/api). A bare host is auto-corrected at build time.
VITE_API_URL=https://ssp-cmms.onrender.com/api

VITE_APP_NAME=CMMS

VITE_SIGNALR_URL=https://ssp-cmms.onrender.com/hubs/work-orders
