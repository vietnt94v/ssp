# Module 11 — Arrays and collections

Chay: `dotnet run --project modules/11-arrays-and-collections`

- Arrays (1D and 2D)
- `List<T>`, `Dictionary<TKey,TValue>`
- `IEnumerable<T>` with `yield return`
