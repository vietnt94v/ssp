# Module 21 — ASP.NET Core minimal API

Chay: `dotnet run --project modules/21-aspnet-minimal-api`

- `WebApplication` / `MapGet` (minimal hosting)
- Dependency injection: `AddSingleton` + constructor injection qua delegate endpoint
- `IConfiguration` doc `appsettings.json` (`Learn:CourseName`)

Sau khi chay, mo `http://localhost:5084/` va `http://localhost:5084/config` (xem port trong terminal neu khac).
